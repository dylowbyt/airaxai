import AdminPanelClient from "@/components/dashboard/AdminPanelClient";

export const metadata = { title: "Admin Panel — AIRAX AI" };

export default function AdminPage() {
  return <AdminPanelClient />;
}
